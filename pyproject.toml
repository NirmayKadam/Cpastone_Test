[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-q"
testpaths = [
  "apps/api-gateway/tests",
  "apps/market-data-service/tests",
  "apps/quant-engine/tests",
  "apps/ml-service/tests",
  "apps/worker-orchestrator/tests",
]

[tool.ruff]
line-length = 100
target-version = "py311"
